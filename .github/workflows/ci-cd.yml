name: CI/CD Pipeline

on:
  push:
    branches: [ development, release ]
  pull_request:
    branches: [ development ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest
        
    - name: Run tests
      run: |
        python -m pytest tests/ -v
        
    - name: Build success
      if: success()
      run: echo "✅ All tests passed!"

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/development'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Simulate deployment
      run: |
        echo "🚀 Starting deployment..."
        echo "Deploying commit: ${{ github.sha }}"
        echo "✅ Deployment completed!"

    documentation:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/development'
    permissions:
      contents: write  # Даем права на запись
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
      
    - name: Generate automated documentation
      run: |
        echo "📚 Generating documentation..."
        mkdir -p docs
        current_time=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
        echo "# 🚀 Automated Build Report" > docs/build_report.md
        echo "" >> docs/build_report.md
        echo "## 📊 Build Information" >> docs/build_report.md
        echo "- **Build Date:** $current_time" >> docs/build_report.md
        echo "- **Commit:** [${{ github.sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})" >> docs/build_report.md
        echo "- **Branch:** ${{ github.ref }}" >> docs/build_report.md
        echo "- **Workflow Run:** [#${{ github.run_number }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> docs/build_report.md
        echo "" >> docs/build_report.md
        echo "## ✅ Test Status" >> docs/build_report.md
        echo "All tests passed successfully! 🎉" >> docs/build_report.md
        echo "" >> docs/build_report.md
        echo "## 📈 Code Metrics" >> docs/build_report.md
        echo "- **Python Files:** $(find . -name '*.py' -not -path './.git/*' | wc -l)" >> docs/build_report.md
        echo "- **Test Files:** $(find tests -name '*.py' | wc -l)" >> docs/build_report.md
        echo "- **Total Lines of Code:** $(find . -name '*.py' -not -path './.git/*' | xargs wc -l | tail -1 | awk '{print $1}')" >> docs/build_report.md
        echo "" >> docs/build_report.md
        echo "---" >> docs/build_report.md
        echo "*Automatically generated by GitHub Actions*" >> docs/build_report.md
        
        echo "✅ Documentation generated successfully"
        
    - name: Commit and push documentation
      run: |
        echo "🚀 Committing documentation updates..."
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Добавляем изменения в документации
        git add docs/
        
        # Проверяем есть ли изменения для коммита
        if git diff --staged --quiet; then
          echo "📝 No changes to commit in documentation"
        else
          git commit -m "📚 docs: auto-update build report for ${{ github.sha }}"
          echo "✅ Changes committed successfully"
          git push
          echo "🚀 Documentation pushed to repository"
        fi
# Добавьте этот job в ваш .github/workflows/ci-cd.yml
  auto-merge:
    needs: [build-and-test, documentation]  # Зависит от успешного выполнения тестов и документации
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.event.pull_request.draft == false
    
    steps:
    - name: Check if PR is mergeable
      id: check-merge
      uses: actions/github-script@v7
      with:
        script: |
          const { data: pr } = await github.rest.pulls.get({
            owner: context.repo.owner,
            repo: context.repo.repo,
            pull_number: context.issue.number
          });
          
          // Проверяем, что PR можно смержить
          if (pr.mergeable && pr.mergeable_state === 'clean') {
            console.log('✅ PR is ready for auto-merge');
            core.setOutput('mergeable', 'true');
          } else {
            console.log('❌ PR is not ready for auto-merge. Status:', pr.mergeable_state);
            core.setOutput('mergeable', 'false');
          }
    
    - name: Auto-merge PR
      if: steps.check-merge.outputs.mergeable == 'true'
      uses: actions/github-script@v7
      with:
        script: |
          try {
            await github.rest.pulls.merge({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
              merge_method: 'squash'  // Можно изменить на 'merge' или 'rebase'
            });
            console.log('✅ PR auto-merged successfully!');
          } catch (error) {
            console.log('❌ Auto-merge failed:', error.message);
          }